Project To-Do List
==================

This list outlines the remaining tasks to bring the Exchange Mini project to a high standard, incorporating best practices for code quality, testing, and developer experience.

Backend (Laravel API):
----------------------
1.  [x] Implement API Resources for `camelCase` Responses:
    *   Create `UserResource` to format user data (e.g., `balance_usd` to `balanceUsd`).
    *   Create `AssetResource` to format asset data (e.g., `locked_amount` to `lockedAmount`).
    *   Create `OrderResource` to format order data.
    *   Apply these resources in `ProfileController`, `AuthController`, `OrderController`, and `OrderMatched` event.
2.  [x] Refactor Controllers with Form Request Classes:
    *   Create `RegisterRequest` for `AuthController::register`.
    *   Create `LoginRequest` for `AuthController::login`.
    *   Create `StoreOrderRequest` for `OrderController::create`.
    *   Create `CancelOrderRequest` (if needed, or use route model binding for ID validation).
3.  [x] Implement Static Analysis (Larastan):
    *   Install `larastan/larastan`, configure `phpstan.neon`, and resolve issues.
4.  [x] Migrate to Pest for Testing:
    *   Install Pest.
    *   Convert existing PHPUnit tests to Pest syntax.
    *   Write comprehensive Unit and Feature tests for all core logic (e.g., MatchingService, Controllers, Models).
5.  [x] Implement OpenAPI (Swagger) Documentation:
    *   Install `darkaonline/l5-swagger` and add annotations to generate `openapi.yaml`.
    *   [x] Generate and run `api-test.sh` to produce `api-output.log` and verify that the live API responses match the generated OpenAPI specification.

Frontend (Vue.js SPA):
----------------------
6.  [x] Implement Pinia for State Management:
    *   [x] Create Pinia stores for `auth`, `profile`, and `orders`.
    *   [x] Integrate stores into Vue components (`Exchange`, `WalletOverview`, `OrderForm`, etc.).
7.  [x] Set up Frontend Testing (Vitest):
    *   [x] Install and configure Vitest.
    *   [x] Write Unit tests for all Pinia stores.
8.  [x] Migrate to TypeScript:
    *   [x] Convert all `.js` files to `.ts` and add type definitions.
    *   [x] Configure ESLint and Prettier for TypeScript.
    *   [x] Achieved 100% unit test coverage for stores.
9.  [ ] E2E Testing (Dusk):
    *   Install Laravel Dusk.
    *   Write E2E tests for critical user flows (registration, login, placing/cancelling orders, matching).

Continuous Integration (CI):
----------------------------
9.  [ ] Finalize GitHub Actions CI Workflow:
    *   Ensure the CI workflow runs Pint, Larastan (once implemented), PHP/Pest tests, and potentially frontend linters/tests.
    *   Add a step to check OpenAPI spec validity if possible.

General:
--------
11. [ ] VSCode Configuration:
    *   Provide `.vscode/settings.json` and `.vscode/extensions.json` for recommended extensions and settings (e.g., auto-formatting on save, Larastan integration).